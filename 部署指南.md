# 题库刷题平台 · 部署与共享指南

让**任何人通过一个免费的公网网址**访问你的平台，并刷到你审核通过的题库。

采用方案：
- **前端**部署在 **GitHub Pages**（免费公网网址，评委点开就能用）
- **题库数据**存 **腾讯云 CloudBase** 云数据库（所有访问者共享同一份题）
- 自动部署：推送到 GitHub 后自动更新网站

---

## 一、接入腾讯云 CloudBase（存题库，让所有人看到你的题）

### 1. 注册腾讯云并开通 CloudBase
1. 打开 https://cloud.tencent.com/product/tcb
2. 用微信/QQ 登录，开通"云开发 CloudBase"
3. 创建一个环境（选免费额度 / 按量付费即可，选**上海**或**广州**地域）

### 2. 拿到环境 ID
- 进入 CloudBase 控制台 → 「环境」→ 复制 **环境 ID**（形如 `xxx-123abc`）

### 3. 开启匿名登录
- 控制台 → 「身份验证」→ 打开 **「匿名登录」** 开关

### 4. 创建题库集合 + 配置读权限
- 控制台 → 「数据库」→ 「新建集合」，集合名填：`question_banks`
- 点击该集合 → 「权限设置」→ 选择 **「所有用户可读，仅创建者可读写」**
  （关键！这样所有访问者都能读到你的题，但只有你能改）

### 5. 把环境 ID 填到项目配置
编辑 `question-master/.env.local`，填入环境 ID：

```env
VITE_CLOUDBASE_ENV_ID=你的环境ID
```

> 本地开发时填到这里；部署到 GitHub 时填到 GitHub Secrets（见下面第三步）。

---

## 二、接入 AI 生成题库（可选，建议）

在 `question-master/.env.local` 填入你的大模型 API：

```env
VITE_AI_API_BASE_URL=https://api.deepseek.com/v1
VITE_AI_API_KEY=你的KEY
```

DeepSeek / 通义千问 / OpenAI 兼容接口均可。

---

## 三、部署到 GitHub Pages（生成公网网址）

### 1. 创建 GitHub 仓库
1. 打开 https://github.com/new
2. Repository name 填：**`question-master`**（必须和 `vite.config.ts` 里的 base `/question-master/` 一致）
3. 选 **Public**（公开，这样评委无需登录就能访问）
4. 创建

### 2. 把项目推到仓库
```bash
cd c:/Users/Lenovo/CodeBuddy/20260819191017/question-master
git init
git add .
git commit -m "题库刷题平台"
git branch -M main
git remote add origin https://github.com/你的用户名/question-master.git
git push -u origin main
```

### 3. 配置 GitHub Secrets（云数据库 + AI 的密钥）
1. GitHub 仓库 → Settings → **Secrets and variables → Actions**
2. 添加这些 **Repository secrets**：
   - `VITE_CLOUDBASE_ENV_ID` = 你的 CloudBase 环境 ID
   - `VITE_AI_API_BASE_URL` = AI 接口地址（可选）
   - `VITE_AI_API_KEY` = AI 密钥（可选）

### 4. 开启 GitHub Pages 自动部署
1. 仓库 → **Settings → Pages**
2. Source 选 **GitHub Actions**（不是"Deploy from a branch"！）
3. 保存

### 5. 触发部署
回到仓库 **Actions** 页，跑一次 `Deploy to GitHub Pages` 工作流（push 后会自动触发）。

等绿勾完成，你的网址就是：
```
https://你的用户名.github.io/question-master/
```

> 之后每次 `git push`，网站都会自动更新。

---

## 四、使用流程（共享你的题库）

### 管理员（你）
1. 打开你的公网网址，右上角点 **「管理后台」**，输 `admin / admin123`
2. 生成题库（建议选 AI 生成）
3. 在 **在线审核** 里批量通过（可多选点「批量通过」）
4. 回到 **题库管理**，点 **「同步题库到云端」**
   → 题库已上云，所有访问者都能刷到！

### 考生（别人/评委）
1. 打开你的公网网址
2. 用账号密码登录（默认体验账号 `student / 123456`，或自己注册）
3. 选择题库开始刷题 —— 刷到的就是你同步上云的题

---

## 常见问题

| 问题 | 处理 |
|------|------|
| 考生登录后看不到题 | 管理员还没点「同步题库到云端」，或 CloudBase 权限没设成"所有用户可读" |
| 同步报错 | 检查 `VITE_CLOUDBASE_ENV_ID` 是否正确、匿名登录是否开启、集合是否创建 |
| 部署后网址 404 | 仓库名必须是 `question-master`，且 Pages 来源要选 GitHub Actions |
| 想改题 | 管理员在后台改完重新点「同步题库到云端」即可 |

---

## 本地开发

```bash
cd question-master
npm install
npm run dev
# 打开 http://localhost:5174
```
